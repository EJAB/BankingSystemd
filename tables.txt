CREATE TABLE EJAB_USERS(
	ID INTEGER,
	SURNAME VARCHAR(30) NOT NULL,
	FIRSTNAME VARCHAR(30) NOT NULL,
	USERNAME VARCHAR(30) NOT NULL,
	PASSWORD VARCHAR(30) NOT NULL,
	AMOUNT INTEGER DEFAULT(0),
	EMAIL VARCHAR(60) NOT NULL,
	PHONE VARCHAR(25),
	PRIMARY KEY (ID)
)

CREATE TABLE EJAB_SENDERS(
	TRANSACTIONID INTEGER,
	SENDERID INTEGER,
	SENDERFIRST VARCHAR(30),
	SENDERLAST VARCHAR(30),
	TRANSFER INTEGER,
	BALANCE INTEGER,
	TRANSACTIONTIME DATETIME,
	TOWARDS VARCHAR(30),
	PRIMARY KEY(TRANSACTIONID),
		CONSTRAINT CONST123
			FOREIGN KEY(SENDERID)
			REFERENCES EJAB_USERS
	)
	
	
insert into ejab_receivers (
	transactionid, 
	receiverid,
	receiverfirst,
	receiverlast,
	transfer,
	balance,
	transactiontime) 
	values(2, 2, 'Mary', 'Poppins', 50, 1450, TO_DATE('2015/05/15 8:30:25', 'YYYY/MM/DD HH:MI:SS'))

ID	FIRSTNAME	SURNAME	EMAIL	USERNAME	PASSWORD	BALANCE
IDTRANSACTIONS	IDRECEIVER	TRANSFERAMOUNT	IDACCOUNT

INSERT INTO EJAB_USERS (SURNAME, FIRSTNAME, USERNAME, PASSWORD, AMOUNT, EMAIL, PHONE) VALUES ('Random', 'Blah', 'blah.random', 'somepassword', 1000, 'blah.random@randomemail.com', '0841234567')